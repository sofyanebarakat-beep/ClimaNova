# Pages

The public route files remain at the project root and in `innerpages/`,
`services/`, `projects/`, `blog/`, and `template-pages/` so existing URLs keep
working on static hosting.

Use this directory for future page-source organization if the project moves to a
build step. Until then, the compatibility route files are the deployable pages.
